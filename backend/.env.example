# ── AI ──────────────────────────────────────────────────────────────────────
# Google Gemini API key (get from https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=your-gemini-api-key-here

# OpenAI API key — used for Whisper audio transcription
# Get from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# ── Confluence ───────────────────────────────────────────────────────────────
# Base URL of your Confluence instance (no trailing slash)
CONFLUENCE_URL=https://your-org.atlassian.net/wiki

# Atlassian account email (used for Basic auth: base64(email:token))
CONFLUENCE_EMAIL=you@your-org.com

# Personal Access Token (or service account token) with page create permissions
CONFLUENCE_TOKEN=your-confluence-pat-here

# Space and parent page are chosen by the user in the UI — no env var needed.

# ── Auth ─────────────────────────────────────────────────────────────────────
# Shared API key checked via X-API-Key header — replace with SSO when ready
ORG_API_KEY=changeme-use-a-long-random-string

# ── Infrastructure ───────────────────────────────────────────────────────────
REDIS_URL=redis://redis:6379/0
DATABASE_URL=sqlite:///./memora.db

# ── Storage ──────────────────────────────────────────────────────────────────
# Local directory for uploaded files (ignored when using S3)
UPLOAD_DIR=uploads

# ── Recording download auth ───────────────────────────────────────────────────
# none  → public URLs and yt-dlp supported platforms (default)
# token → Webex org service account; requires WEBEX_SERVICE_TOKEN below
# oauth → per-user OAuth flow (not yet implemented)
RECORDING_AUTH_MODE=none

# Required only when RECORDING_AUTH_MODE=token
WEBEX_SERVICE_TOKEN=

# Optional: path to a Netscape-format cookies.txt for YouTube authentication.
# Only needed for sign-in-gated videos (members-only, age-restricted).
# Export from Chrome via the "Get cookies.txt LOCALLY" extension.
YOUTUBE_COOKIES_FILE=

# ── Webex Webhook Integration ────────────────────────────────────────────────
# Bot token used to download recordings via the Webex Recordings API.
# Create a bot at developer.webex.com and copy its access token here.
WEBEX_BOT_TOKEN=

# Optional: secret set when registering the Webex webhook.
# If set, the X-Spark-Signature header (HMAC-SHA1 of the raw body) is
# validated on every inbound event.  Leave blank to skip validation (local dev).
WEBEX_WEBHOOK_SECRET=

# ── Email notifications ──────────────────────────────────────────────────────
# Sent after a successful Confluence publish (webhook-originated jobs only).
# Uses stdlib smtplib — no extra dependencies.
# Leave SMTP_HOST blank to disable email notifications entirely.

# SMTP server hostname (e.g. smtp.gmail.com, smtp.sendgrid.net)
SMTP_HOST=

# Port: 587 = STARTTLS (recommended), 465 = SSL, 25 = plain / local relay
SMTP_PORT=587

# SMTP credentials — leave blank for unauthenticated relays (e.g. MailHog)
SMTP_USER=
SMTP_PASSWORD=

# Envelope From address shown to recipients
SMTP_FROM=memora@yourdomain.com

# Public base URL of the Memora frontend — used in email links
MEMORA_BASE_URL=http://localhost:5173

# ── CORS ─────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins
CORS_ORIGINS=http://localhost:5173,http://localhost:80
